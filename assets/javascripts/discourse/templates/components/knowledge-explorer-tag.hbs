<a {{action "selectTag"}} class="chord-directory-item chord-directory-tag {{if tag.active 'selected'}} {{if subtag 'subtag'}}">
  {{#unless tag.active}}
    {{d-icon "plus"}}
  {{/unless}}
  {{#if tag.active}}
    {{d-icon "times-circle"}}
  {{/if}}
  <span class="chord-directory-item-id tag-id">{{tag.id}}</span>
  <span class="chord-directory-item-count tag-count">{{tag.count}}</span>
</a>
